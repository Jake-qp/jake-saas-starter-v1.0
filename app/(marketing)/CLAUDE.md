# Marketing Pages — AI Instructions

## Rules

1. Use `<PricingCard>` from `@/components/PricingCard` for pricing sections.
2. Use semantic color tokens — no raw Tailwind colors (ESLint enforced).
3. Use `cn()` from `@/lib/utils` for class merging.
4. Icons from `@radix-ui/react-icons` only.
5. Responsive: mobile-first design with `sm:`, `md:`, `lg:` breakpoints.
